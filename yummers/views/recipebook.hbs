 <div class="container w-70  current-black py-5 center">
    <div class="row" style="border-bottom: 10px solid #A1A2A3;">
        <h1 class="col-lg-12 page-header ">RECIPE BOOK</h1>
    </div>
    <div class="row" style="height:50px;"></div>
    {{#if recipes}}
        <div class="display-res ml-5 pl-5">
            {{#each recipes}}
                {{> recipe_card username = ../user.name users = ../users}}
            {{/each}}
        </div>
    {{else}}
        <div class="row mx-5 text-black">
            <p>It seems like you don't have any saved recipes! <a href="/">Click here</a> to browse recipes!</p>
        </div>
    {{/if}}
</div>